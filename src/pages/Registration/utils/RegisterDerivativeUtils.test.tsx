import RegisterDerivativeUtils from "./RegisterDerivativeUtils";

describe("RegisterDerivativeUtils", () => {
  it("should be defined", () => {
    expect(RegisterDerivativeUtils).toBeDefined();
  });
});